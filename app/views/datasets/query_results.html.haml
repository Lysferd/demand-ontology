= content_for( :subtitle ) do
  Resultado da Query SPARQL

%p
  Query inserida:
%table
  %tr
    %td= @query

%p
  Resultado:
%table
  - for result in @results do
    %tr
      - for column in result.to_s.split( /(\(.*?\))/ ) do
        %td= column