%h2
  Listing Datasets
%table
  %thead
    %tr
      %th
        Nome
      /%thRDF de Origem
      %th
        Opções
  %tbody
    - @datasets.each do | dataset |
      %tr
        %td= dataset.name
        /%td= link_to dataset.rdf_source, "/datasets/#{dataset.name}/#{dataset.rdf_source}"
        %td
          = button_get 'Detalhes', dataset
          = button_get 'Editar', edit_dataset_path( dataset )
          = button_get 'Destruir', dataset

= button_get 'Novo Dataset', new_dataset_path
